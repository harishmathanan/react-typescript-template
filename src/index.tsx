import * as React from 'react';
import * as ReactDOM from 'react-dom';

import './app.css';

ReactDOM.render(
  <h1 className="header">React TypeScript Template</h1>,
  document.getElementById('root') as HTMLDivElement
);
